import './poker.css';
import PokerGame from '../components/game/PokerGame';

export const metadata = { title: 'Texas Hold\'em — sundol' };

export default function GamePage() {
  return <PokerGame />;
}
